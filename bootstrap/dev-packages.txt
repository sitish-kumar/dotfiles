# OPT-IN development software (captured from the reference machine).
# NOT installed by default. Install with:  ./install.sh --dev   (or: ./install.sh --dev --no-system)
# Same format as packages.txt: one package per line, `aur:` prefix needs yay,
# comments on their own line.

# --- editors / IDEs ---
aur:visual-studio-code-bin
vim

# --- version control ---
github-cli

# --- languages / runtimes ---
nodejs
npm
python-pip
jdk17-openjdk

# --- containers (enable the service after: sudo systemctl enable --now docker) ---
docker
docker-compose

# --- databases ---
postgresql

# --- mobile / android ---
android-tools
aur:android-studio

# --- face unlock (PAM) — heavy: pulls python-dlib -> cuda + cudnn (~6 GB) ---
# Re-enroll per machine: `sudo howdy add`; set [video] device_path in
# /etc/howdy/config.ini to that laptop's IR/webcam. See system/ + ARCH-INSTALL.md.
aur:howdy-git
