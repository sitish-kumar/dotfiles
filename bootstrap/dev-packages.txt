# OPT-IN development software (captured from the reference machine).
# NOT installed by default. Enable the dev phase with:  ./install.sh --dev
#   manual mode (-i)      -> asked per group below
#   --dev --with-optional -> install every group
#   --dev (automatic)     -> install every group (--dev is the explicit opt-in)
#   --dev --no-optional   -> consider nothing
# Format: same `# group: name | desc` sections as optional-packages.txt.
# One package per line; `aur:` prefix needs yay; bare `#` lines are ignored.

# group: editors | VS Code + vim + GitHub CLI
aur:visual-studio-code-bin
vim
github-cli

# group: languages | Node, Python, Java toolchains
nodejs
npm
python-pip
jdk17-openjdk

# group: containers | Docker + compose (enable after: sudo systemctl enable --now docker)
docker
docker-compose

# group: databases | PostgreSQL
postgresql

# group: android | adb/fastboot tools + Android Studio
android-tools
aur:android-studio

# group: face-unlock | Howdy IR face unlock — HEAVY: pulls python-dlib -> CUDA + cuDNN (~6 GB)
# Re-enroll per machine: `sudo howdy add`; set [video] device_path in
# /etc/howdy/config.ini to that laptop's IR/webcam. See system/ + ARCH-INSTALL.md.
aur:howdy-git
