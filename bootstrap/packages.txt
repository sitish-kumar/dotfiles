# Packages for this Hyprland + Quickshell(ii) setup (Arch).
# Format: one package per line; comments on their OWN line (never inline).
# Lines starting `aur:` need an AUR helper (yay). install.sh parses this.
#
# SELF-CONTAINED: we own the CONFIG (vendored in config/). The desktop's deps come
# from the `illogical-impulse-*` AUR meta-packages below — these pull the full
# qt6 / fonts / python / portal / quickshell stack transitively. No separate
# "run the end-4 installer" step is needed; `install.sh` installs everything here.
# (The ii *config* = our repo; `illogical-impulse-quickshell-git` = the ii-flavored
# Quickshell *runtime* — different things.)

# --- illogical-impulse meta-packages (the base; pull most deps transitively) ---
aur:illogical-impulse-basic
aur:illogical-impulse-hyprland
aur:illogical-impulse-quickshell-git
aur:illogical-impulse-toolkit
aur:illogical-impulse-widgets
aur:illogical-impulse-python
aur:illogical-impulse-audio
aur:illogical-impulse-backlight
aur:illogical-impulse-portal
aur:illogical-impulse-screencapture
aur:illogical-impulse-kde
aur:illogical-impulse-fonts-themes
aur:illogical-impulse-bibata-modern-classic-bin
aur:illogical-impulse-microtex-git

# --- theming ---
aur:matugen-bin

# --- launcher / clipboard / input (list explicitly; --needed dedupes) ---
cliphist
wl-clipboard
wtype
ydotool
fuzzel

# --- terminals (launch_first_available) ---
foot
kitty

# --- plugin build toolchain (hyprpm + hyprtasking fork) ---
base-devel
cmake
meson
cpio
pkgconf
git

# --- small utilities referenced by scripts ---
jq
bc
rsync
