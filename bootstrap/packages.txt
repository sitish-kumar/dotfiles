# Packages for this Hyprland setup (Arch).
# Format: one package per line. Comments on their OWN line (never inline).
# Lines beginning with `aur:` are AUR packages (installed via an AUR helper).
# install.sh parses this; don't put trailing comments after a package name.
#
# IMPORTANT: this layers ON TOP of the end-4 / illogical-impulse base. For a
# truly fresh machine, run the upstream dots-hyprland dependency install first
# (it pulls the large qt6/python/font/portal set this config assumes), THEN
# clone this repo and run ./install.sh. See README.

# --- compositor + session ---
hyprland
hypridle
hyprlock
xdg-desktop-portal-hyprland
polkit-gnome
pipewire
wireplumber
gnome-keyring

# --- theming ---
aur:matugen-bin

# --- shell / launcher / clipboard / input ---
aur:quickshell-git
cliphist
wl-clipboard
wtype
ydotool
fuzzel

# --- terminals (launch_first_available) ---
foot
kitty

# --- plugin build toolchain (hyprpm + hyprtasking fork) ---
base-devel
cmake
meson
cpio
pkgconf
git

# --- utilities referenced by scripts/configs ---
jq
bc
rsync
python
xorg-xrdb
geoclue
